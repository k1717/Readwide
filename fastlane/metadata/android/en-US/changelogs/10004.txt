Readwide 1.0.4
- Reworked single-page PDF zoom: double-tap and pinch keep the tapped spot under your finger, no jump or flicker, with immediate page-turn taps (continuous mode unchanged).
- Moved to a new applicationId, com.readwide.manager, finishing the TextView Reader rename; 1.0.4 installs as a separate app, so use JSON backup export/import to migrate.
- Unified reader search (case, whole-word, regex, nth-result) across TXT, Markdown, EPUB, HWP/HWPX, Word; fixed clipped settings rows.
