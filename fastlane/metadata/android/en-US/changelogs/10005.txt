Readwide 1.0.5
- Text-to-speech adds a sleep timer (Off/15/30/45/60 min or custom) counting playback time only, finishing the current sentence first.
- Real pause/resume keeps your place across app exits, plus a draggable floating play/pause/stop card.
- TTS keeps reading while scrolling, pauses for calls and headphone unplug, stops when another app takes over audio.
- Multi-select delete warns when folders are included; fixed two PDF crashes and TTS resume rewinding; faster regex rules.
